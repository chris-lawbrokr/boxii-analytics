/**
 * THE single brand purple for the whole app.
 *
 * Change this one value to re-tint every brand data mark in the dashboard: all
 * charts, every breakdown bar, and the conversion funnel. This is the only place
 * the purple is defined.
 *
 * Note: the click heatmap uses its own classic multi-hue scale (blue→red) and is
 * intentionally NOT driven by this — see app/components/OverlayHeatmap.tsx.
 */
export const BRAND_PURPLE = "#5e3794";
